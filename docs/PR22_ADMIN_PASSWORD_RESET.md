# PR-22 Admin Password Reset

- Shop Admin and Super Admin can reset a selected tenant user's password.
- A dedicated password reset panel requires confirmation.
- Temporary passwords can be generated, shown, hidden, and copied.
- Password reset is separate from role and permission updates.
- Every successful reset writes an audit log without storing the password.
