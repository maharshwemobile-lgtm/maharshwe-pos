# Tenant Verification Notes

Production rollout requires PostgreSQL and authenticated requests. Verify the current shop reports zero relation violations before release. Users are deactivated, not permanently removed, so historical cashier references remain available in Sale History.
